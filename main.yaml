- hosts: all
  become: true
  tasks:
    - name: install qemu guest agent
      package:
        name: qemu-guest-agent
        state: present

    - name: enable and start agent
      service:
        name: qemu-guest-agent
        enabled: yes
        state: started

